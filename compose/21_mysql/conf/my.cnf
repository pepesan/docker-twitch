# For advice on how to change settings please see
# http://dev.mysql.com/doc/refman/8.4/en/server-configuration-defaults.html

[mysqld]
#
# Remove leading # and set to the amount of RAM for the most important data
# cache in MySQL. Start at 70% of total RAM for dedicated server, else 10%.
# innodb_buffer_pool_size = 128M
#
# Remove leading # to turn on a very important data integrity option: logging
# changes to the binary log between backups.
# log_bin
#
# Remove leading # to set options mainly useful for reporting servers.
# The server defaults are faster for transactions and fast SELECTs.
# Adjust sizes as needed, experiment to find the optimal values.
# join_buffer_size = 128M
# sort_buffer_size = 2M
# read_rnd_buffer_size = 2M

host-cache-size=0
skip-name-resolve
datadir=/var/lib/mysql
socket=/var/run/mysqld/mysqld.sock
secure-file-priv=/var/lib/mysql-files
user=mysql
pid-file=/var/run/mysqld/mysqld.pid

## Configuración para el log de consultas lentas
# Activar el log de consultas lentas
slow_query_log = 1

# Ruta del archivo (dentro del contenedor)
slow_query_log_file = /var/lib/mysql/slow-queries.log

# Tiempo en segundos para considerar una consulta "lenta"
long_query_time = 2

# Registrar consultas que no usan índices (opcional pero recomendado)
log_queries_not_using_indexes = 1
[client]
socket=/var/run/mysqld/mysqld.sock

!includedir /etc/mysql/conf.d/

